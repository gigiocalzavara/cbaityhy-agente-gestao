"use client";
import { useEffect, useState } from "react";
import { appPath } from "@/lib/base-path";
export default function ProcessingPage(){const[data,setData]=useState<any>(null);useEffect(()=>{fetch(appPath("/api/admin/overview"),{cache:"no-store"}).then(r=>r.json()).then(setData)},[]);return <main className="admin-dashboard"><header><span>OPERAÇÃO DE DADOS</span><h1>Processamentos</h1><p>Execuções internas das buscas ativas das 04h e indicadores das 05h.</p></header><section className="admin-panel" style={{maxWidth:1280,margin:"0 auto"}}><table className="admin-simple-table"><thead><tr><th>Data</th><th>Município</th><th>Rotina</th><th>Status</th><th>Concluídas</th><th>Linhas</th><th>Erro</th></tr></thead><tbody>{data?.runs?.map((run:any)=><tr key={run.id}><td>{new Date(run.created_at).toLocaleString("pt-BR")}</td><td>{data.municipalities.find((m:any)=>m.id===run.municipality_id)?.name||"Geral"}</td><td>{run.routine==="active_search"?"Buscas ativas · 04h":"Indicadores · 05h"}</td><td><span className={"admin-pill "+run.status}>{run.status}</span></td><td>{run.completed_tasks}/{run.total_tasks}</td><td>{run.row_count}</td><td>{run.error_message||"—"}</td></tr>)}</tbody></table></section></main>}
